# تورنکس — راهنمای پروژه برای Claude Code

## این پروژه چیه
سایت شرکتی تورنکس: عمده‌فروش و تامین‌کننده تجهیزات فیبر نوری، تجهیزات شبکه، و سیم و کابل (تمرکز ویژه روی خراسان افشارنژاد). مشتری‌ها B2B هستن (پیمانکار، پروژه‌های عمرانی، فروشگاه‌های زنجیره‌ای)، نه خرده‌فروشی مستقیم.

## تصمیمات معماری (قطعی — تغییر نده مگر کاربر بخواد)
- **پلتفرم:** وردپرس self-hosted. نه Elementor، نه هیچ page builder دیگه.
- **بلاک‌ها/چیدمان (تغییر‌کرده روز ۲):** ACF Blocks کنار گذاشته شد — پلاگین ACF رایگان از Blocks/Repeater/Gallery/Options Page پشتیبانی نمی‌کنه (اینا فقط تو ACF PRO هستن؛ کاربر لایسنس PRO نداره). به‌جاش: بلاک‌های بومی گوتنبرگ (Group/Cover/Columns/Heading/Paragraph/Buttons/Image) چیده‌شده در قالب **Block Patterns** (ثبت‌شده با PHP در `patterns/{name}.php`، بدون نیاز به build tooling یا JS). ظاهر برند از طریق `theme.json` (پالت رنگ، فونت، تایپوگرافی) تزریق می‌شه تا کنترل‌های خود ادیتور (رنگ/فونت/spacing) هماهنگ با برند باشن.
- **تنظیمات سراسری سایت** (تلفن/ایمیل/آدرس/شبکه‌های اجتماعی): از طریق Customizer (Appearance → Customize)، نه ACF Options Page.
- **فرم تماس:** شورت‌کد PHP (`[tornex_contact_form]`) + هندلر `admin-post.php` (نه بلاک ACF) — چون فرم نیاز به منطق سمت سرور داره.
- **محصولات:** Custom Post Type (`product`) + تاکسونومی (`product_category`) + فیلدهای ساده‌ی ACF رایگان (فقط Text، نه Repeater/Gallery) برای مشخصات فنی؛ گالری تصویر و توضیحات از طریق ادیتور بلاکی خود پست (Gallery بلاک بومی + WYSIWYG).
- **محیط توسعه:** لوکال روی سیستم کاربر (wp-env، با Docker Desktop) قبل از هرچیزی روی هاست زنده.
- **کنترل نسخه:** گیت، یک commit جدا بعد از هر بخش تکمیل‌شده و تست‌شده.
- **هدف نهایی ادیت:** کاربر باید بتونه محتوا و چیدمان هر بخش رو داخل ادیتور بلاکی وردپرس (Gutenberg) ویرایش کنه، بدون باز کردن کد — پترن‌ها دقیقاً همین رو با بلاک‌های بومی (که خودشون قابل جابه‌جایی/تکرار/حذف تو ادیتورن) تامین می‌کنن.

## اسکوپ نسخه‌ی اول (MVP — نقطه‌ی شروع، ددلاین فوری زیر یک هفته)
اسکوپ اولیه فقط ۴ صفحه بود (صفحه اصلی، درباره ما، تماس، تمپلیت محصول)، ولی پروژه از اون فراتر رفته — الان بلاگ، مگامنو، ۴ فرم درخواست و سئو هم اضافه شدن. جزئیات کامل و به‌روز تو «وضعیت فعلی پروژه» پایین همین فایله.

هنوز خارج از اسکوپ: چندزبانگی، عکس‌های نهایی واقعی (همچنان استوک/جایگزین تا کاربر عکس واقعی بده).

## منابع پروژه (حتماً قبل از شروع بخون)
- `docs/project-plan.md` — نقشه‌ی کامل پروژه، جدول زمانی ۷ روزه، وضعیت هر فاز
- `docs/content-and-brand.md` — محتوای نهایی هر ۴ صفحه (فارسی)، پالت رنگ، فونت
- `design/homepage-preview.html` — پیش‌نمایش تایید‌شده‌ی طراحی صفحه اصلی (نسخه‌ی v1، مرجع بصری برای رنگ/فونت/لحن — نه پیکسل‌به‌پیکسل قطعی، ولی جهت طراحی همینه)

## پالت رنگ (از لوگو)
- قرمز برند: `#B5121B` | قرمز تیره/هاور: `#8A0D15`
- مشکی/ناوبری: `#1C1B1B` | خاکستری متن: `#5B5B5B`
- پس‌زمینه روشن: `#F7F6F5` | سفید: `#FFFFFF`

## فونت
Vazirmatn (فارسی، از Google Fonts) — برای هدینگ و بدنه هر دو.

## محیط توسعه لوکال
- `npm install` یک‌بار، بعد `npm start` (یا `npx wp-env start`) برای بالا آوردن سایت.
- سایت: http://localhost:8888 — ادمین: http://localhost:8888/wp-admin (کاربر: `admin` / رمز: `password`)
- تم پروژه (`tornex`) و پلاگین ACF به‌صورت خودکار موقع `start` فعال می‌شن (`.wp-env.json` → `lifecycleScripts`).
- ریشه‌ی ریپازیتوری خودش تم وردپرسه (`style.css`, `functions.php`, `theme.json` در ریشه) — پیچیدگی اضافه‌ی nested theme folder نداریم.
- `patterns/{name}.php` — الگوهای بلاکی (خودکار از `functions.php` رجیستر می‌شن). `fields/{name}.php` — فیلدگروپ‌های ACF (روی محصول یا ترم تاکسونومی `product_category`، نه بلاک‌ها). `inc/` — کد PHP کمکی (Customizer، فرم تماس، فرم‌های درخواست، CPT محصول).
- `npx wp-env run cli wp <command>` برای اجرای دستورات wp-cli داخل کانتینر.
- `npm stop` برای متوقف کردن، `npx wp-env destroy` برای پاک کردن کامل (دیتابیس هم می‌ره).

## قواعد کدنویسی
- هر پترن تو `patterns/{pattern-name}.php` (کامنت هدر با Title/Slug/Description/Categories مثل پترن‌های استاندارد وردپرس).
- بعد از تکمیل و تست هر بخش، یک commit جدا بزن (پیام کامیت: `feat(name): توضیح کوتاه`).
- محتوای فارسی رو مستقیم از `docs/content-and-brand.md` کپی کن، دوباره ننویس.
- عکس‌ها فعلاً placeholder/stock — با یه کامنت `<!-- TODO: replace with real photo -->` مشخص کن تا بعداً راحت پیدا بشه.
- قبل از رفتن سراغ بخش بعدی، بخش فعلی رو تو مرورگر لوکال تست کن (curl/wp-cli برای بررسی خطای PHP + بازدید واقعی صفحه).

## وضعیت فعلی پروژه

سایت از اسکوپ اولیه‌ی MVP (۴ صفحه) فراتر رفته و الان یه سایت کامل و کاربردیه. زیرساخت + همه‌ی صفحات اصلی + کاتالوگ محصول + بلاگ + مگامنو + ۴ فرم درخواست + سئو، همه ساخته، تست (curl/wp-cli/screenshot + بررسی debug.log بعد از هر تغییر) و commit شدن. کار بعدی: **انتقال به هاست زنده (روز ۶)** — منتظر تایید کاربر، و تکمیل موارد «باز» زیر که به محتوای واقعی از کاربر نیاز دارن.

### ساخته و تست شده
- **زیرساخت:** pattern auto-loader، theme.json (پالت/فونت/تایپوگرافی)، Customizer (تماس/شبکه‌های اجتماعی/صفحات کلیدی)، CPT محصول + تاکسونومی دو-سطحی (`product_category`، ۴ دسته اصلی + ۲۲ زیردسته واقعی)، ACF (فیلدهای متنی مشخصات فنی + آیکون دسته‌بندی + دیتاشیت فایل).
- **۴ صفحه اصلی + بلاگ:** صفحه اصلی، درباره ما، تماس، و بلاگ (`home.php`/`single.php`، صفحه Posts Page، ۱۰ پست جای‌گیر Lorem Ipsum).
- **محصولات:** ۲۶ محصول واقعی (نام/توضیحات/مشخصات فنی مستقل نوشته‌شده، بدون عکس) در ۴ دسته × ۲۲ زیردسته؛ تمپلیت single/archive/دسته‌بندی، دکمه دیتاشیت (فعال/غیرفعال بر اساس فیلد ACF).
- **هدر:** مگامنوی هاوری (دسته → گرید زیردسته با آیکون، از تاکسونومی واقعی)، دکمه‌های لیست قیمت/صدور پیش‌فاکتور/استعلام قیمت.
- **۴ فرم درخواست** (`inc/lead-forms.php`, شورت‌کد پارامتری‌شده‌ی `[tornex_lead_form type="..."]`): خرید عمده، خرید همکار/نمایندگی، خرید سازمانی، لیست قیمت و پیش‌فاکتور (دوستونه با آیکون تماس سریع + کپچای ریاضی داخلی). فیلد «اقلام درخواستی» جست‌وجوی زنده‌ی محصول واقعیه (AJAX از `product` post type، نه لیست ثابت).
- **صفحه اصلی:** بخش «آخرین مقالات»، بنر میان‌صفحه با عکس تیم، اسلایدر نظرات مشتریان + گرید برندهای همکار (جای‌گیر).
- **فوتر:** ۵ ستون (لوگو/توضیح/شبکه‌های اجتماعی، دسترسی سریع به همه صفحات، خرید و همکاری، تماس، نقشه).
- **سئو:** Yoast SEO نصب/تنظیم — عنوان/توضیحات برای ۴ صفحه اصلی + هر ۴ دسته‌بندی، تمپلیت خودکار برای محصولات، sitemap فعال.
- **پنل مدیریت:** به فارسی سوییچ شد (fa_IR core + پلاگین‌ها)؛ راهنمای کامل ویرایش در `docs/wp-admin-editing-guide.md`.

### موارد باز — منتظر محتوای واقعی از کاربر
| مورد | کجا | وضعیت فعلی |
|---|---|---|
| لیست برندهای همکار | `patterns/testimonials.php` | ۶ باکس جای‌گیر با الگوی راه‌راه |
| عکس واقعی تیم/انبار | `assets/img/team-cta-bg.jpg` | عکس استوک Unsplash |
| عکس محصولات | هر محصول (Featured Image) | فال‌بک عکس استوک بر اساس دسته |
| فایل دیتاشیت هر محصول | فیلد ACF `datasheet` روی هر محصول | خالی → دکمه «به‌زودی» |
| آیکون هر دسته/زیردسته مگامنو | فیلد ACF `category_icon` روی ترم (Products → Categories) | آیکون پیش‌فرض placeholder |
| متن «مشتریان ما» (لوگو/عدد) | `patterns/about.php` | متن جای‌گیر |
| نام/شرکت واقعی نظرات مشتریان | `patterns/testimonials.php` | متن نقل‌قول نهایی، ولی نام «مشتری نمونه» |
| ۱۰ پست بلاگ | نوشته‌ها | فقط Lorem Ipsum، برای بررسی چیدمان |
| سرویس SMTP | خارج از کد، تنظیم هاست | فعلاً `wp_mail()` لوکال بدون transport کار نمی‌کنه (تست شد، همون رفتار رو فرم تماس قدیمی هم داره) |
| دامنه و اطلاعات هاست زنده | خارج از کد | هنوز مشخص نشده |

### تصمیم‌های فنی مهمی که این سشن گرفته شد
- تاکسونومی `product_category` دو-سطحی شد (۴ دسته + ۲۲ زیردسته واقعی بر اساس نوع محصول) تا مگامنو و صفحات دسته‌بندی معنادار باشن؛ آرشیو دسته‌ی والد خودکار محصولات زیردسته‌ها رو هم نشون می‌ده (رفتار پیش‌فرض وردپرس).
- دو ترم قدیمی بی‌ربط تاکسونومی («۳»/«۵») که از قبل این سشن‌ها مونده بودن و باعث خرابی مگامنو می‌شدن، حذف شدن.
- Permalink structure این نصب لوکال یادش رفته بود «plain» تنظیم بشه؛ فیکس شد و برای پایداری بعد از `wp-env destroy` به `lifecycleScripts` در `.wp-env.json` هم اضافه شد.
- `tornex_fa_digits()` از تابع محلی داخل یه پترن به helper مشترک تو `functions.php` منتقل شد (چون جاهای دیگه‌ای مثل کپچا هم بهش نیاز داشتن).
- **⚠️ نکته‌ی مهم برای هر کار بعدی رو تاکسونومی:** چون هر محصول به یه ترم فرزند (نه والد) اساین شده، هر جا که نیاز به «دسته‌ی کلی محصول» داری (مثل fallback عکس استوک یا کلید فیلتر) باید حتماً `tornex_top_level_category( $term )` رو صدا بزنی تا به والد سطح‌بالا برسی — مستقیم از `get_the_terms()[0]->name` استفاده نکن، چون اون همیشه ترم فرزنده و با نگاشت ۴تایی `tornex_category_stock_image()`/`tornex_category_filter_key()` مچ نمی‌شه (این دقیقاً همون باگی بود که همه‌ی عکس‌های محصول رو یکی کرد و فیلتر تب‌ها رو خراب کرد — فیکس شد، ولی برای کد جدید هم همین قانون رو رعایت کن).

### نکته درباره‌ی docs/design-references/
دو فایل (`megamenu-brands.png`، `preinvoice-form-reference.png`) اسکرین‌شات اشتباهی از خود این سشن Claude Code‌ان، نه رفرنس طراحی واقعی — دست نخوردن چون فایل کاربرن، ولی برای طراحی بی‌فایده‌ن. رفرنس واقعی فرم پیش‌فاکتور تو `megamenu-brands-2.png` هست (اسمش اشتباهه ولی محتواش درسته).
