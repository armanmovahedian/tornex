# تورنکس — راهنمای پروژه برای Claude Code

## این پروژه چیه
سایت شرکتی تورنکس: عمده‌فروش و تامین‌کننده تجهیزات فیبر نوری، تجهیزات شبکه، و سیم و کابل (تمرکز ویژه روی خراسان افشارنژاد). مشتری‌ها B2B هستن (پیمانکار، پروژه‌های عمرانی، فروشگاه‌های زنجیره‌ای)، نه خرده‌فروشی مستقیم.

## تصمیمات معماری (قطعی — تغییر نده مگر کاربر بخواد)
- **پلتفرم:** وردپرس self-hosted. نه Elementor، نه هیچ page builder دیگه.
- **بلاک‌ها/چیدمان (تغییر‌کرده روز ۲):** ACF Blocks کنار گذاشته شد — پلاگین ACF رایگان از Blocks/Repeater/Gallery/Options Page پشتیبانی نمی‌کنه (اینا فقط تو ACF PRO هستن؛ کاربر لایسنس PRO نداره). به‌جاش: بلاک‌های بومی گوتنبرگ (Group/Cover/Columns/Heading/Paragraph/Buttons/Image) چیده‌شده در قالب **Block Patterns** (ثبت‌شده با PHP در `patterns/{name}.php`، بدون نیاز به build tooling یا JS). ظاهر برند از طریق `theme.json` (پالت رنگ، فونت، تایپوگرافی) تزریق می‌شه تا کنترل‌های خود ادیتور (رنگ/فونت/spacing) هماهنگ با برند باشن.
- **تنظیمات سراسری سایت** (تلفن/ایمیل/آدرس/شبکه‌های اجتماعی): از طریق Customizer (Appearance → Customize)، نه ACF Options Page.
- **فرم تماس:** شورت‌کد PHP (`[tornex_contact_form]`) + هندلر `admin-post.php` (نه بلاک ACF) — چون فرم نیاز به منطق سمت سرور داره.
- **محصولات:** Custom Post Type (`product`) + تاکسونومی (`product_category`) + فیلدهای ساده‌ی ACF رایگان (فقط Text، نه Repeater/Gallery) برای مشخصات فنی؛ گالری تصویر و توضیحات از طریق ادیتور بلاکی خود پست (Gallery بلاک بومی + WYSIWYG).
- **محیط توسعه:** لوکال روی سیستم کاربر (wp-env، با Docker Desktop) قبل از هرچیزی روی هاست زنده.
- **کنترل نسخه:** گیت، یک commit جدا بعد از هر بخش تکمیل‌شده و تست‌شده.
- **هدف نهایی ادیت:** کاربر باید بتونه محتوا و چیدمان هر بخش رو داخل ادیتور بلاکی وردپرس (Gutenberg) ویرایش کنه، بدون باز کردن کد — پترن‌ها دقیقاً همین رو با بلاک‌های بومی (که خودشون قابل جابه‌جایی/تکرار/حذف تو ادیتورن) تامین می‌کنن.

## اسکوپ نسخه‌ی اول (MVP — ددلاین فوری، زیر یک هفته)
فقط ۴ صفحه، انیمیشن ساده:
1. صفحه اصلی/لندینگ
2. درباره ما
3. تماس با ما (فرم تماس کارکردی)
4. تمپلیت محصول (یک الگوی تکرارپذیر برای همه محصولات)

خارج از اسکوپ فعلی: بلاگ، چندزبانگی، انیمیشن‌های پیچیده، عکس‌های نهایی واقعی (فعلاً استوک/جایگزین چون کاربر عکس واقعی نداره).

## منابع پروژه (حتماً قبل از شروع بخون)
- `docs/project-plan.md` — نقشه‌ی کامل پروژه، جدول زمانی ۷ روزه، وضعیت هر فاز
- `docs/content-and-brand.md` — محتوای نهایی هر ۴ صفحه (فارسی)، پالت رنگ، فونت
- `design/homepage-preview.html` — پیش‌نمایش تایید‌شده‌ی طراحی صفحه اصلی (نسخه‌ی v1، مرجع بصری برای رنگ/فونت/لحن — نه پیکسل‌به‌پیکسل قطعی، ولی جهت طراحی همینه)

## پالت رنگ (از لوگو)
- قرمز برند: `#B5121B` | قرمز تیره/هاور: `#8A0D15`
- مشکی/ناوبری: `#1C1B1B` | خاکستری متن: `#5B5B5B`
- پس‌زمینه روشن: `#F7F6F5` | سفید: `#FFFFFF`

## فونت
Vazirmatn (فارسی، از Google Fonts) — برای هدینگ و بدنه هر دو.

## محیط توسعه لوکال
- `npm install` یک‌بار، بعد `npm start` (یا `npx wp-env start`) برای بالا آوردن سایت.
- سایت: http://localhost:8888 — ادمین: http://localhost:8888/wp-admin (کاربر: `admin` / رمز: `password`)
- تم پروژه (`tornex`) و پلاگین ACF به‌صورت خودکار موقع `start` فعال می‌شن (`.wp-env.json` → `lifecycleScripts`).
- ریشه‌ی ریپازیتوری خودش تم وردپرسه (`style.css`, `functions.php`, `theme.json` در ریشه) — پیچیدگی اضافه‌ی nested theme folder نداریم.
- `patterns/{name}.php` — الگوهای بلاکی (خودکار از `functions.php` رجیستر می‌شن). `fields/{name}.php` — فیلدگروپ‌های ACF (فقط برای محصول، نه بلاک‌ها). `inc/` — کد PHP کمکی (Customizer، فرم تماس، CPT محصول).
- `npx wp-env run cli wp <command>` برای اجرای دستورات wp-cli داخل کانتینر.
- `npm stop` برای متوقف کردن، `npx wp-env destroy` برای پاک کردن کامل (دیتابیس هم می‌ره).

## قواعد کدنویسی
- هر پترن تو `patterns/{pattern-name}.php` (کامنت هدر با Title/Slug/Description/Categories مثل پترن‌های استاندارد وردپرس).
- بعد از تکمیل و تست هر بخش، یک commit جدا بزن (پیام کامیت: `feat(name): توضیح کوتاه`).
- محتوای فارسی رو مستقیم از `docs/content-and-brand.md` کپی کن، دوباره ننویس.
- عکس‌ها فعلاً placeholder/stock — با یه کامنت `<!-- TODO: replace with real photo -->` مشخص کن تا بعداً راحت پیدا بشه.
- قبل از رفتن سراغ بخش بعدی، بخش فعلی رو تو مرورگر لوکال تست کن (curl/wp-cli برای بررسی خطای PHP + بازدید واقعی صفحه).

## وضعیت فعلی پروژه
- [x] محتوا و پالت رنگ/فونت تایید شده (روز ۱)
- [x] راه‌اندازی محیط لوکال وردپرس + نصب ACF + git init (روز ۲)
- [~] ساخت بخش‌های اسکوپ MVP (روز ۳-۴) — در حال انجام:
  - [x] زیرساخت: pattern auto-loader، theme.json، Customizer تنظیمات سایت، شورت‌کد فرم تماس + هندلر admin-post، CPT محصول + تاکسونومی + فیلدگروپ مشخصات فنی (همه تست‌شده، بدون خطای PHP)
  - [x] پترن‌های صفحه اصلی (هیرو، ارزش‌های پیشنهادی، دسته‌بندی، چرا تورنکس، نظرات مشتریان، بنر CTA) — همه تست‌شده رو صفحه اصلی (page ID 6، تنظیم‌شده به‌عنوان front page)، commit جدا برای هرکدوم. پالیش بصری (فاصله‌گذاری، آیکون‌باکس، گرادیانت هیرو، سواچ رنگی دسته‌بندی) و فیکس باگ سایز فونت (theme.json defaultFontSizes) انجام شد.
  - [x] پترن صفحه درباره ما (page ID 13) — متن اصلی + تکرار پترن ارزش‌های پیشنهادی + بخش مشتریان (جای‌گیر)
  - [x] پترن صفحه تماس (page ID 14) — متن مقدمه، اطلاعات تماس از Customizer، شورت‌کد فرم تماس، جای‌گیر نقشه
  - [x] هدر/فوتر واقعی — هدر: wp_nav_menu (primary) + دکمه CTA؛ فوتر: گرید ۴ ستونه با اطلاعات تماس از Customizer. تنظیمات جدید Customizer: tornex_contact_page / tornex_about_page (dropdown-pages)
  - [x] تمپلیت single/archive برای محصول (breadcrumb، گالری/توضیحات از محتوای بلاکی، جدول مشخصات فنی ACF، CTA، محصولات مرتبط)
- [x] روز ۵ — تکمیل شد:
  - [x] بازبینی ریسپانسیو — موبایل با DevTools چک و تأیید شد (بدون بریدگی/مشکل نمایش). فیکس‌های دفاعی: فونت H1 موبایل، سایز لوگو هدر، flex-wrap، overflow-x.
  - [x] تزریق محتوای نهایی:
    - تلفن (۰۹۳۰۴۱۰۲۶۵۸ / ۰۹۱۲۹۲۲۹۹۵۸)، ایمیل (info@tornex.ir)، آدرس (تهران، لاله‌زار، قلب بازار برق ایران، کوچه مهنا) — از طریق Customizer (`wp theme mod set`)، نه هاردکد؛ فوتر و صفحه تماس خودکار از `get_theme_mod` می‌خونن.
    - نظرات مشتریان: تگ جای‌گیر حذف شد — کاربر تایید کرد این متن‌ها محتوای نهایی‌ان.
    - بخش «تیم تورنکس» (۵ اسم، بدون سمت) به صفحه درباره‌ما اضافه شد.
  - باقی‌مونده‌ی شناخته‌شده (خارج از اسکوپ این تزریق): بخش «مشتریان ما» تو صفحه درباره‌ما هنوز جای‌گیره (لوگو/تعداد مشتریان واقعی)؛ عکس محصولات هم همچنان placeholder.
- [~] پاس نهایی جذابیت بصری (بعد از روز ۵، قبل از روز ۶) — در حال انجام، مرحله‌به‌مرحله با commit جدا بعد از هر مرحله:
  - [x] **مرحله ۱ — انیمیشن (تکمیل، commit `1d47dd0`):** Vanilla JS خالص در `assets/js/animations.js` (enqueue شده تو `functions.php`)، بدون کتابخانه‌ی جدید:
    - `.tornex-animate` + IntersectionObserver → fade-in/slide-up موقع اسکرول رو کارت‌های ارزش پیشنهادی/دسته‌بندی/چرا-تورنکس (متن+آمار)/نظرات مشتریان، با تأخیر پلکانی بین کارت‌های هر گرید (`transition-delay` inline، محاسبه‌شده تو حلقه‌ی PHP هر پترن).
    - `.tornex-counter` + `data-counter-target/prefix/suffix` → شمارشگر انیمیشنی آمار (+۸، ۱۰۰٪، ۲۴/۷) با ارقام فارسی؛ «LC» چون عدد نیست شمارش نداره.
    - `.tornex-texture-grid` (CSS، در `global.css`) → بافت گرید ظریف قرمز-برند رو هیرو و بخش چرا-تورنکس.
    - هاور کارت‌ها/دکمه‌ها پررنگ‌تر شد. `prefers-reduced-motion` رعایت می‌شه.
    - تست‌شده: اسکرین‌شات صفحه اصلی + بدون خطای PHP رو صفحه اصلی/درباره‌ما/تماس.
  - [x] **مرحله ۲ — عکس‌های stock (تکمیل، commit `28b02e3`):** ۵ عکس رایگان از Unsplash (لایسنس Unsplash License، بدون نیاز به attribution) در `assets/img/`، سبک تیره/حرفه‌ای بدون لوگوی شرکت ثالث:
    - `category-fiber.jpg` (فیبر نوری)، `category-network.jpg` (تجهیزات شبکه/رک سرور)، `category-cable.jpg` (سیم و کابل)، `category-other.jpg` (سایر تجهیزات کابل)، `about-warehouse.jpg` (انبار/عملیات).
    - هیرو (`patterns/hero.php` از طریق `.tornex-hero` تو `global.css`): عکس فیبر نوری زیر همون گرادیانت تیره‌ی موجود (فقط شفافیت گرادیانت تنظیم شد تا عکس زیرش دیده بشه؛ بافت گرید مرحله ۱ دست‌نخورده باقی موند).
    - دسته‌بندی محصولات (`patterns/categories.php`): هر کارت به‌جای گرادیانت رنگی ساده، عکس متناظر با موضوعش می‌گیره (`background-image` این‌لاین، از آرایه‌ی `$tornex_categories`)؛ overlay تیره‌ی `::before` موجود برای خوانایی متن حفظ شد.
    - درباره ما (`patterns/about.php`): بنر عکس انبار/عملیات (`.tornex-about-photo`) بین متن معرفی و بخش «چرا تورنکس» اضافه شد.
    - تمپلیت محصول (`single-product.php`, `archive-product.php`) + تابع کمکی `tornex_category_stock_image()` تو `functions.php`: وقتی محصولی عکس شاخص نداره، عکس استوک متناظر با نام دسته‌بندیش (map بر اساس نام فارسی ۴ دسته) نشون داده می‌شه به‌جای متن ساده‌ی جای‌گیر؛ روی صفحه‌ی تک‌محصول یه بج «نمونه تصویر — عکس واقعی به‌زودی» هم روی عکس میاد تا کاربر گیج نشه که این عکس واقعی محصوله.
    - همه‌ی جاهای استفاده با کامنت `<!-- TODO: replace with real photo -->` مشخص شدن.
    - تست‌شده: لینت PHP بدون خطا، بدون خطای جدید تو debug.log، اسکرین‌شات هدلس Chrome از صفحه اصلی (هیرو+دسته‌بندی) و درباره‌ما، و یک محصول تست موقت (با تاکسونومی ترم واقعی) برای تست مسیر fallback تمپلیت محصول — بعد از تایید بصری، محصول و ترم تست حذف شدن، هیچ داده‌ای باقی نموند.
  - [x] **مرحله ۳ — محصولات نمونه:** به‌جای این‌که جدا انجام بشه، ضمن کار «۵ تغییر مهم» زیر تکمیل شد (چون بخش‌های ۳/۴ اون کار مستقیم به داده‌ی محصول واقعی نیاز داشتن) — جزئیات کامل زیر بخش «۵ تغییر مهم بعد از پاس بصری».
  - [x] **مرحله ۴ — بازبینی نهایی:** هر ۴ صفحه‌ی اصلی + صفحات محصول (تک/آرشیو) در طول کار «۵ تغییر مهم» به‌طور مکرر تست شدن (curl/wp-cli/اسکرین‌شات)، بدون خطای PHP.
- [x] **۵ تغییر مهم بعد از پاس بصری** (درخواست جدید کاربر، بعد از تکمیل پاس نهایی جذابیت بصری) — همه‌ی ۵ بخش تکمیل و commit جدا شدن:
  - [x] **۱. فیکس لینک‌های مرده (commit `1ba9b7c`):** ۳ دکمه (هیرو ×۲، بنر CTA) که به `href="#"` می‌رفتن، حالا به صفحه تماس/آرشیو محصولات وصلن. هدر/فوتر از قبل درست بودن (تست شد). باگ ریشه‌ای هم فیکس شد: پترن‌ها رو هوک `init` زودتر از CPT محصول اجرا می‌شدن (`tornex_register_patterns` به‌جای اولویت پیش‌فرض، اولویت ۲۰ گرفت) — این باعث می‌شد `get_post_type_archive_link('product')` داخل پترن‌ها همیشه خالی برگرده.
  - [x] **۲. جلوه‌ی نوری هیرو (commit `b425947`):** CSS-only، بدون JS/فایل اضافه — Ken Burns آروم رو عکس پس‌زمینه (`background-size` انیمیشن‌شده، ۲۶ ثانیه) + خط‌های نور مورب متحرک (`::before` با `repeating-linear-gradient` + `mix-blend-mode:screen`، ۲۲ ثانیه). `prefers-reduced-motion` رعایت شد.
  - [x] **۳ و ۴. محصولات پرطرفدار + جست‌وجوی هوشمند (commit `0a17891`):**
    - پیش‌نیاز: ۴ دسته‌بندی واقعی محصول + ۱۰ محصول نمونه (۳ فیبر نوری، ۳ تجهیزات شبکه، ۲ سیم و کابل خراسان، ۲ سایر تجهیزات) با مشخصات فنی واقع‌گرایانه (استانداردهای بین‌المللی واقعی) مستقیم تو دیتابیس ساخته شد (نه هاردکد تو کد، چون محتواست). فیلد «برند» عمداً خالی موند چون برند واقعی نداریم.
    - `patterns/popular-products.php`: بین دسته‌بندی و چرا-تورنکس، ۶ محصول اخیر با `WP_Query` ساده، بدون JS.
    - `patterns/product-finder.php`: زیرش، تب‌های فیلتر دسته‌بندی (vanilla JS show/hide کلاینت‌ساید، بدون AJAX) + CTA «دنبال کاتالوگ یا دیتاشیت خاصی هستی؟» که به فرم تماس با `?catalog=1` وصل می‌شه و پیام پیش‌فرض تو `تورنکس/inc/contact-form.php` پر می‌کنه.
    - تابع کمکی جدید `tornex_category_filter_key()` تو `functions.php` (کلید انگلیسی پایدار برای فیلتر، چون اسلاگ فارسی وردپرس درصد-انکود می‌شه).
  - [x] **۵. پاس مدرن‌سازی (commit `df1adac`):** مشکل اصلی گزارش‌شده — کارت‌ها flat بودن (فقط سایه موقع hover، نه در حالت استراحت) که حس ارزون می‌داد. توکن‌های سایه (`--shadow-sm/md/lg`) به `:root` اضافه شد و به همه‌ی کارت‌ها (ارزش پیشنهادی، نظرات، دسته‌بندی، محصولات پرطرفدار/فیلتر، مرتبط، گالری محصول، فرم تماس) اعمال شد؛ شعاع گردی کارت‌ها از ۱۰-۱۲px به ۱۴px یکدست شد (هم CSS هم attribute بلاک Gutenberg)؛ باکس‌های آماری چرا-تورنکس (رو پس‌زمینه‌ی تیره) به‌جای سایه یه پرشدگی سفید ظریف گرفتن؛ هدر sticky سایه گرفت؛ عکس‌های کارت محصول/مرتبط saturate/contrast ملایم گرفتن.
  - نکته‌ی جانبی کشف‌شده (خارج از اسکوپ، دست نزدم): دو ترم قدیمی و بی‌ربط تو تاکسونومی `product_category` هست (id=4 نام «۳»، id=6 نام «۵») که مال قبل از این سشن بودن — احتمالاً باقی‌مونده‌ی تست یه سشن خیلی قبل‌تر. اگه خواستی پاکشون کنم بگو.
- [ ] انتقال به هاست زنده (روز ۶) — شروع نشده، منتظر تایید کاربر
- [ ] پابلیش نهایی (روز ۷)
